# TripleO config ceph client ansible role
A role for TripleO deployed openstack, config ceph client for cinder, gnocchi,
nova-compute, glance.

# Version
Pike

# Requirement
Ansible >= 2.4.1

# Install roles
